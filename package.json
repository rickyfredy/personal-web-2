{
  "name": "personal-web-2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONT,BACK -c blue,green \"npm run --prefix frontend dev\" \"cd backend && python3 main.py\"",
    "dev:frontend": "npm run --prefix frontend dev",
    "dev:backend": "cd backend && python3 main.py",
    "build": "npm run --prefix frontend build",
    "start": "npm run --prefix frontend start",
    "install:all": "npm install --prefix frontend && cd backend && pip3 install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
